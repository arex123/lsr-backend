// const mongoose = require('mongoose');

// const problemSchema = new mongoose.Schema({
//   email: { type: String, required: true },
//   problemId: { type: String, required: true },
//   repetitionCount: { type: Number, default: 0 },  // Tracks how many times the user has solved this problem
//   difficulty: { type: String, required: true },   // 'easy', 'medium', 'hard'
//   status: { type: String, default: 'ongoing' },   // 'ongoing' or 'completed'
//   lastSolvedDate: { type: Date, default: Date.now },
// });

// const Problem = mongoose.model('Problem', problemSchema);

// module.exports = Problem;

